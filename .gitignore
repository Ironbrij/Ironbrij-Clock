# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env files — .env only ever held the public anon key/URL historically, but
# nothing should stop that from changing; keep secrets out of git going
# forward and let .env.example carry the shape for onboarding instead.
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Rollback snapshots written by the one-time duplicate-entry cleanup; they hold
# real time-entry rows for every user, so they stay out of git.
duplicate-merge-backup-*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Supabase CLI local state (holds a pooler URL and project ref)
supabase/.temp/
